language: c
compiler: clang
os: osx


before_install:
  - autoheader && aclocal && automake --add-missing && automake && autoconf

script:
  - ./configure && make

env:
  global:
    - CACHE="$HOME/.local"


matrix:
  include:
    - os: linux
      sudo: required
      dist: trusty
