#include <stdio.h>
#include <ardp/hashmap.h>

int main( int argc, char **argv ) {
        map_t *map = map_create( 1 );

        if ( map is NULL ) {
                return -1;
        }

        return 0;
}
